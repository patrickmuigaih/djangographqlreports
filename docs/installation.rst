============
Installation
============

At the command line::

    $ easy_install graphql_reports

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv graphql_reports
    $ pip install graphql_reports
